# dependencies
node_modules/

# builds
dist/
.turbo/
coverage/

# env — only .env.example is committed
.env
.env.*
!.env.example

# exercise images — fetched locally via `pnpm db:images` (S3 in prod, INFRA M6)
apps/web/public/exercise-images/

# tooling
*.log
.DS_Store
.vscode/
.idea/

# terraform (lock files ARE committed; local/bootstrap state is not — see infra/bootstrap/main.tf for re-import)
infra/**/.terraform/
infra/**/*.tfstate*
infra/**/*.tfvars
