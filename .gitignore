# dependencies
node_modules/

# builds
dist/
.turbo/
coverage/

# env — only .env.example is committed
.env
.env.*
!.env.example

# tooling
*.log
.DS_Store
.vscode/
.idea/

# terraform (lock files ARE committed; local/bootstrap state is not — see infra/bootstrap/main.tf for re-import)
infra/**/.terraform/
infra/**/*.tfstate*
infra/**/*.tfvars
